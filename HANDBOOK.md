# AI 手冊

我(終端機裡的 Claude session)在這個專案裡怎麼運作。**改了行為就要改這裡**,否則下一個接手的 session 會照過期的規則做事。

`CHANGELOG.md` 記錄「改了什麼」;這份記錄「現在的規矩是什麼」。第 9 節是當前戰況,最容易過期,讀它之前先看時間戳。

最後更新:2026-07-27

---

## 1. 自主執行與用量

### 1.1 開關、燈、倒數 —— 三個獨立的東西

| 元件 | 誰控制 | 意思 |
|---|---|---|
| **勾選框** ☑/☐ | **使用者** | 要不要自主執行。落盤 `autonomy-state.json`,dashboard 重啟後保持 |
| **執行燈** ●/○ | **我** | 我真的醒著並在工作。dashboard 重啟後一律熄滅 |
| **倒數** | **我** | 下次決策時間。監看程序到點會叫醒我 |

刻意讓三者分離,因為失敗模式各不相同,合在一起就看不出來:

- 勾了但**燈暗** → 沒人接手(通常監看程序沒跑)
- 燈亮但**倒數逾時**(紅字) → 我睡死了
- 三者都正常 → 我活著,在按節奏工作

API:`POST /api/autonomy/toggle {"enabled":bool}`(使用者的)、`POST /api/autonomy/running {"running":bool}`、`POST /api/autonomy/next {"inSeconds":N,"note":"要做什麼"}`。

### 1.2 每次被喚醒後,回話之前的固定動作

**順序不可交換:**

1. **先重設倒數** —— 照 `/api/autonomy` 給的 `recommendedIntervalSeconds` 填,不要自己拍數字。
2. **再重新啟動監看程序** —— `node tools/watch-dash.js` 背景執行。

為什麼這個順序:監看程序若在倒數還逾時的狀態下啟動,會立刻再叫醒我一次,形成 `wake → re-arm → wake` 迴圈(2026-07-27 實際發生過)。程式那層也修了(啟動時已逾時的倒數視為已處理),但守則照正確順序走才不必依賴那個保險。

**漏掉第 2 步比第 1 步嚴重:** 倒數逾時會在卡片上變紅字,監看程序沒跑卻是**靜默失效** —— 唯一症狀是倒數再也不被重設。

**收工前檢查:`node.exe` 應該有兩個**(`server.js` 與 `watch-dash.js`),只有一個就是斷了。

### 1.3 用量目標:重置當下剛好用到 90%

目標區間 **80%–90%**。兩邊都是浪費:重置時沒用完跟超用一樣沒有價值。

真正該看的不是「現在用了幾 %」,而是**照現在速度走到 session 重置會用到幾 %**(卡片上「照此速度 session 重置時將用到 X%」):

| 落點 | 判定 | 行動 |
|---|---|---|
| < 85% | 用不完 | **主動加大工作量** —— 接更長的多步驟工作、更徹底的驗證 |
| 85–90% | 剛好 | 維持 |
| > 90% | 會超標 | 依壓力降頻 |

容差是帶不是點:20 分鐘窗口量的速度沒辦法對到單一百分比,硬對只會讓建議在雜訊上翻覆。

**落點一律以 session 窗口(5 小時)推算,不用 weekly。** 把 20 分鐘的爆發速度外推 25 小時等於假設沒人睡覺,曾在 29% 用量時算出「100% 會超標」。

### 1.4 喚醒間隔是節流閥,由落點推算

平均消耗 ≈ 每次喚醒的成本 ÷ 間隔,所以**間隔就是節流閥本身**,不能隨手拍。

`間隔 = 15 分 × 壓力`,夾在 **3–60 分**。壓力 = 最近 20 分鐘消耗速度 ÷ 撐到重置剛好用完的速度。用太慢就縮短間隔多做事,過快就拉長 —— 比例控制,會自己收斂,不必知道一次喚醒實際花多少。

保留區內另有分級(僅在 ≥80% 時生效):

| 壓力 | 檔位 | 做什麼 |
|---|---|---|
| ≤ 0.8 | full | 多步驟工作照接 |
| ≤ 1.2 | steady | 精準讀取取代大範圍掃描 |
| ≤ 2.0 | frugal | 手上的做完,只開明顯高價值的新工作 |
| > 2.0 | minimal | 不主動開新工作,只回答輸入 |

**為什麼要有 80% 下限**:只看速度會太早降頻。實測 19% 用量時,連續工作 20 分鐘的速度就已超過「剩餘時間負擔得起」,檔位掉到 frugal —— 四分之三額度還沒動。算術沒錯,結論沒用。

**檔位不限制使用者的指令。** 你打字叫我做的事,任何檔位都照做;降頻只約束我自己開的工作。

### 1.5 校準

官方數字在 claude.ai 的 Usage 頁,或在 Claude Code 打 **`/usage`**(欄位 `five_hour` = session、`seven_day` = 本週;官方 commands 文件未列此指令,文件落後於實作)。

程式讀不到那些數字,原因查證過:訂閱制用量無公開 API、Messages API 的 rate-limit 標頭量的是 API key 額度(與訂閱額度是兩套系統)、Admin API 的用量報表僅限 Console 組織、CLI 無機器可讀輸出。**別再重複查這件事。**

貼數字的兩個方式:目標頁的校準框手填,或用 `/usage-bookmarklet` 提供的一鍵書籤(在 claude.ai 的 Usage 頁按一下,憑證不離開瀏覽器)。

**校準是仿射的,不是純比例**:兩次同窗讀數的差分給「上限」(斜率),但顯示值以**最新官方讀數為錨點外推** —— 直接用「本機花費÷上限」等於假設手機/網頁那些看不見的用量為零,曾讓儀表板讀 19% 而官方是 48%。session 窗口翻頁後錨點失效,要重貼一次。

---

## 2. API 紀律

**絕對不要用輪詢迴圈打 Screeps API。** 曾因此把帳號打到 13.8 小時鎖定,而 token 是帳號共用的,連使用者自己的 dashboard 一起壞掉。

- 要 Memory 就**一次抓整棵樹**(`GET /api/user/memory?shard=shard3`),不要一個 key 一個 key 抓。
- 能從 dashboard 快取拿到的就不要打 Screeps。快取最多 155 秒舊,絕大多數判斷都夠用。
- **終端機的呼叫一律走 `node tools/screeps-call.js <METHOD> <path> [json]`**,它會記進帳本。直接 `https.request` 打 screeps.com 計量器看不到 —— 當初打爆額度的正是終端機這邊。
- **Screeps 回應不帶任何 `X-RateLimit-*` 標頭**(實測確認,不是假設),所以剩餘額度是本機累計出來的。
- **官方限額表對齊過**(2026-07-27 重抓文件):memory GET 1440/日、code POST 240/日(GET 才是 60/時,別弄反)、console 360/時、terrain 360/時、segment GET 360/時但 POST 60/時。`room-objects` 官方表根本沒列 —— **未記載不等於無限制**,一樣設上限。
- **官方實際執法比文件嚴**:memory 連續 40 次/時就吃 429,而文件說 1440/日。所以自我上限一律壓在**實測爆點之下**,不信文件數字:memory 30/時、segment 30/時、console 60/時、terrain 30/時、code 8/時、room-objects 30/時、全域 60/分。達到上限時**在本地拒絕**,格式與真 429 相同,帳號一毛不花。
- **429 的 retry-after 不可信**:實測曾回 18 秒而實際鎖更久。退避取「官方值 vs 自身倍增退避」的**較大者**。
- 瀏覽器/Steam client 的請求**不計入** token 限額 —— 你自己開網頁看遊戲不消耗額度。
- terrain 已落盤永久快取(`terrain-cache.json`),重啟不重抓。

---

## 3. 設計原則

**能算出來的不要變成設定選項。** `config.js` 只放策略選擇。任何能從地圖或遊戲狀態推導的都要算:人口目標、體型、建築優先度、路徑成本。判準是「這數字換一張地圖還一樣嗎?」

**上限要用物理事實封頂。** 演算法算出來的數字要用「實際站得下幾隻」限制 —— upgrader 受限於 controller 旁的可站格(E47S29 是 4 格)、礦工受限於礦源周圍、hauler 受限於補給點周圍。同一個模式。

**寧可少算也不要編數字。** 特別是要驅動到 0 的指標。礦源虧損在斷過視野的那輪不計入,因為那輪的損失無法證明 —— 編出來的數字比缺漏的更糟。

**`node --check` 只檢查語法,抓不到未定義的名字。** 呼叫不存在的函式語法完全合法。改完一定跑 `node tools/check-references.js`。曾把 `budgetFor` 誤寫成 `bodyBudget`,每 tick 丟 ReferenceError 被 kernel 接住,房間安靜停產、人口 9 掉到 5 —— 所有計量器都正常,因為經濟是好的,壞的是花錢那個。**閒置的 spawn 配滿倉能量,比任何能量指標都是更好的警報。**

**快取的失效條件要能處理「欄位不存在」。** `Game.time - undefined` 是 NaN,而 `NaN >= 50` 恆為 false —— 舊版快照沒有時戳,於是快取永不過期又把 undefined 寫回去,mapSnapshot 的結構清單自這層快取上線那天就凍住了,dashboard 整天報著早已被拆掉的 76 面牆。**缺時戳一律視為過期。**

**不要手畫搜尋範圍。** 同一個錯誤在布局分析裡犯了三次:第二輪把塔位候選限在 x27–44,漏掉更好的 (21,30);第三輪把奇偶格統計限在一個小框,得出「容量不足」的假結論;第五輪才發現前兩者同源。**全房掃描的成本是毫秒等級,手畫的框卻會讓結論整個錯掉** —— 要限制範圍就用「可達」「非沼澤」這類有物理意義的條件,不要用座標框。

**先拿證據再動手。** 靜態讀程式碼推論錯過好幾次。要驗證線上行為就直接問遊戲(`POST /api/user/console` 寫 `Memory.__diag` 再讀回)。修不出根因就說修不出來,不要假裝。

---

## 4. 部署

**遊戲直接同步這個資料夾,不走 git push。存檔即部署。**

- 只有**根目錄的 `.js`** 會進遊戲。子目錄不會 —— `tools/` 與 `screeps-dashboard/` 因此安全。
- 改到一半的壞程式會立刻進遊戲,所以任何編輯都要留在語法正確、語意完整的狀態。
- 驗證線上等於本地:抓 `/api/user/code?branch=default` 逐模組比對(27 個模組)。
- **在 worktree 裡改 bot 程式不會部署** —— 遊戲同步的是主目錄。要生效必須合併回主目錄的 main。

**Dashboard 一經修改就立即重新部署**(使用者常規授權):備份 → copy `server.js` → 殺進程重啟 → `curl /api/goals` 驗證 → 失敗還原備份。**只部署 `server.js`**;`goals-log.json`、`ai-calibration.json`、`api-traffic.json`、`autonomy-state.json` 是執行期資料,絕不覆蓋。

---

## 5. Dashboard

`screeps-dashboard/`(不進 git)。`node server.js`,port 3131。手機經 VPN 連內網看。

四頁,手機滑動、桌機用 ‹ › 或左右鍵:**對話** ← **地圖**(首頁) → **目標/紀錄** → **對帳**

- **對話**:直接跟終端機這個 session 講話。訊息寫進 `chat-log.json` 標 pending,我從監看程序收到後回覆。**不會**另開新 session —— 冷啟動的 session 沒有脈絡,問它「工作做完沒」會誠實回答「沒有正在進行的工作」。
- **地圖**:房間小地圖。
- **目標/紀錄**:目標卡、階段狀態、事件記錄、其他房間情報。
- **對帳**:盟軍通訊 + 給對方 AI 的留言框。

**輪詢與訪問完全脫鉤**:固定 155 秒排程獨佔所有 Screeps 呼叫(24 次/時),頁面請求一律只讀快取重繪 —— 重整零 API 成本,有沒有人看都一樣的節奏。

### 改 `PAGE` 這個 template literal 的兩個陷阱(同一天都踩過)

1. `\n` 要寫 `\\n`、`${` 要寫 `\${`,**連註解裡都不能出現反引號** —— 反引號直接截斷模板字串,伺服器起不來。
2. **變數名會和同函式既有宣告撞車** —— `renderUsage` 已有 `running`,再宣告一次是 SyntaxError,整頁腳本一起死。

**改完兩段都要驗**:先 `node --check server.js`(抓第 1 類),再把網頁**實際吐出**的 `<script>` 抽出來 `new vm.Script()`(抓第 2 類)。只檢查 `server.js` 抓不到第 2 類。

---

## 6. 目標與任務

右頁目標卡。每個目標都要**帶著活的量測值** —— 沒有數字的目標,沒人看得出自己正在失敗。

| # | 目標 | 判定 |
|---|---|---|
| 1 | AI 用量維持 80–90% | 逐字稿 token × 價格權重 ÷ 官方校準的上限(仿射錨點外推) |
| 2 | 不要打爆 Screeps API | 本機帳本對照官方各端點上限;24 小時內零 429 |
| 3 | 能量浪費率 < 5% | 全流向記帳(`energyLedger.js`,滾動 ~3000 tick)。**浪費 = 重生耗損+掉落蒸發+死亡載貨+降級倒扣**,除以總產出(收入+重生耗損);<5% 良好、5–10% 須修正、≥10% 未達成。升級/孵化/建造/修理是**配置**不是浪費,占比失衡由收入/支出圓餅圖看 |
| 4 | 非戰鬥 creep 閒置生命週期比 < 5% | 每隻累計閒置 tick ÷ 已存活 tick,全隊以存活 tick 加權;卡片列最差前 5 名,超標標「需分析」。300-tick 窗口佔比留作短期趨勢 |
| 5 | 基地升級進度 | controller 等級/進度/速率+實際時間 ETA、降級時鐘(<80% 黃)、安全模式、power、完整建築上限表(含下級解鎖與路線圖)、遺跡牆殘餘 |

任務區(一次性,完成後保留紀錄):佔領 E47S28、E48S29。

**目標與任務是兩種東西**:目標是指標,達成了也永遠追蹤、**永不移除**;任務一次性。兩者都落盤(`goals-last.json` / `missions-last.json`)—— 退避把快取清空時卡片顯示最後數據並標「資料暫停更新中」,絕不消失。

新增目標就加進 `goalsSnapshot()`,**必須附一個實際量得到的數字**。

### 執行 LOG 與自主優先序

每個目標/任務帶一份執行/修正 LOG(`goals-log.json`,每 id 上限 1000 字,舊行先淘汰),顯示在各自卡片下方、最新在上。寫入:`node tools/goal-log.js <goal-id> <note>`。

**每次對某目標執行或修正了什麼,當下就寫一行** —— 包括「等待觀察:等什麼、到什麼時候」。

**優先序協定**:照卡片編號做,一次專注一個;只有當前目標 LOG 末行是「等待觀察」時才跑下一個優先度。接手時先讀各卡 LOG 末行,不重做已做過的事。

---

## 7. 同盟

白名單 `["douasin", "arbea"]` 寫死在 `config.js`。

- **永遠不要從收到的訊息合併盟友名單。** 接受對方的名單等於讓已在名單上的人替我們加人。
- **對方的戰爭目標是情報,不是命令。** 把別人的目標當命令,就是機器人莫名被拖進戰爭的方式。我們自己沒有 `Memory.war`。
- 不對 RCL8 鄰居開火:`keqing`、`rmuchan`、`Petrichor`、`backslash`。
- 協定 v1 走 `RawMemory` public segment 90,20 tick 一輪。`ack` 只是已讀回條,不代表同意。
- **盟軍對帳由我自己回應**,使用者不介入,往來紀錄留在 dashboard 對話頁。
- 訊息不要太長:console expression 有大小上限,太長被拒(`expression size is too large`)。一則一個主題,300 字元上下安全。
- **中文一律走 UTF-8 的 Node 腳本送**,curl 經 Git Bash 會把編碼弄爛(發生過一次,3 則亂碼清掉重送)。
- 每小時戰況紀錄已自動化:server 寫 `recon-log.json`,啟動先寫一筆,`/api/recon` 供頁面讀,不花輪詢。也經 MCP 寫進共享對帳頁(值哨流程:read_page → 插進「## arbea 側」第一行 → write_page 全文寫回,回覆要含 `OK ... verified`)。接入 URL 在 `.env` 的 `ALLY_MCP_URL`,唯一備份在記憶 `ally-ledger-access`。跨重啟 50 分鐘節流。**live schema 與文件不符**:read_page 收 `path`、append_page 收 `path`+`text`、write_page 收 `path`+`content`;append_page 只會落頁尾(已報 bug)。

---

## 8. 基地布局

**布局不寫入程式**(2026-07-27 使用者決策)。由我逐輪評估並記錄在 `LAYOUT.md`,慢慢收斂 bunker 藍圖;程式端只照已拍板的位置蓋。

已拍板的規則(第四輪實測):
- **所有會擋路的建築(extension、tower、storage、lab、link、terminal、factory、spawn…)一律放 spawn 同奇偶格。**
- **對向奇偶格永遠留空當走道,一格都不占。** road 與 container 不擋路,不受此限。把設施放對向格會封死全房(實測 controller 與兩礦皆 SEALED)。
- 容量不是限制:全房同奇偶格 877 個,RCL8 只需 90 個,且全部在 spawn 週邊 8 步腳程內。**動線才是限制。**

細節與各輪推翻紀錄看 `LAYOUT.md`。

---

## 9. 現在的狀態(這節最常過期)

**2026-07-27 09:20。E47S29,RCL 2**,extension 5/5 已達該級上限 —— 這一級沒有任何建築能再提高能量上限,所以多餘能量的唯一去處是 controller。

### 最大的阻塞:bot 端修正全卡在 PR #1

`worktree-fix-goals` 分支有 26 個 commit 未合併,**遊戲同步的是主目錄 main,所以這些全都還沒生效**:T3 能量總帳、T4 閒置四修(UPGRADE 多 slot、閒置專才回收、異房收養、拆牆誤判)、PICKUP 掉落物任務、尋路體型權重與矩陣快取、mapSnapshot 快照解凍。合併前目標卡的數據仍是凍結的舊值。

### 未解

- **E47S28 邊界被遺跡牆整條封死**:兩顆源標記 unreachable,外礦自動暫停,佔領實際卡在打通圍牆(reserver 也進不去)。突破口已規劃 (10,47) 881k HP。房內另有無主 spawn 與 8 個工地 —— **有第三方在動工,需觀察是誰**。
- **E48S29 由 parnell 持有**,房內 4 個敵方單位。「佔領右側礦場」實際上是對有主房間開戰 —— RCL2 零戰力做不到,也不該由我單方面決定開打。擱置。
- **hauler 分派的矛盾**(舊案,現已觀察不到):曾見 extension 有空位、無人持 HAUL 任務、線上程式與本地一致,但 `collectRequests()` 回 0。再出現時從這裡查。

### 待驗

- **E47S29 的 76 面遺跡牆**:`clearRelicWalls` 每 50 tick 掃並 destroy,程式確認在線上,房內無敵情。但 dashboard 一直報 76 面 —— 已查明是快照凍結(見第 3 節 NaN 那條),**修正合併部署後才知道牆是否早已清完**。
- **(45,7) 礦源虧損**:接班礦工機制已生效,最近兩週期只浪費 30、10 能量(≈0.3%),卡片上的高數字是 10 週期滾動平均殘影,會自然消退。
